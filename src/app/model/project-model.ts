export interface Project {
  tabId: string;
  displayText: string;
}

export interface ProjectItem {
  projectId: number;
  category: string;
  imageURL: string;
  title: string;
  description: string;
}
